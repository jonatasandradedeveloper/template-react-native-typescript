import React from 'react'
import { Text, SafeAreaView } from 'react-native'

export function Home() {
    return (
        <SafeAreaView style={{alignItems: 'center', justifyContent: 'center', flex: 1}}>
            <Text>Template React Native With Typescript</Text>
        </SafeAreaView>
    )
}
